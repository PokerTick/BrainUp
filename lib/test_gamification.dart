import 'dart:convert';
import 'package:http/http.dart' as http;
import 'dart:io';

void main() async {
  // get token from some known source or print out the shape if we can't.
  // Wait, I can't easily get the auth token without Flutter's SharedPreferences.
}
